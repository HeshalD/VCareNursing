# Production build config (used automatically by `vite build`).
# Relative path: the browser hits the same origin, host Nginx forwards to the
# frontend container, whose Nginx proxies /api/ -> backend:5000 (see nginx.conf).
VITE_API_URL=/api
